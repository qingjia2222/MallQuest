<view class="page"><view class="card"><view class="title">五种规划场景</view><view class="chips"><view wx:for="{{scenes}}" wx:key="scene" class="chip" data-i="{{index}}" bindtap="run">{{item.name}}</view></view></view><view class="card"><view class="muted">IDLE → UNDERSTAND → COLLECT → PLAN → ROUTE → CONFIRM → EXECUTE → DONE</view><view class="result">{{result}}</view><button wx:if="{{canConfirm}}" class="button" bindtap="confirm">明确确认并执行</button></view></view>
