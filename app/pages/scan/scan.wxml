<view class="scan-page">
  <particle-intro play="{{playing}}" text="{{connected ? '已连接本商场' : '正在连接'}}" bind:done="onParticleDone" />
  <view class="skip" wx:if="{{connected}}" bindtap="enterHome">进入 AI 助手 ›</view>
</view>
