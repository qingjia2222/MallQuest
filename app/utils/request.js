function request(path,{method='GET',data={}}={}){const app=getApp();return new Promise((resolve,reject)=>wx.request({url:app.globalData.apiBase+path,method,data,header:{'content-type':'application/json',Authorization:app.globalData.token?`Bearer ${app.globalData.token}`:''},success(res){const body=res.data||{};if(res.statusCode>=200&&res.statusCode<300&&body.code===0)resolve(body.data);else reject(new Error(body.message||`HTTP ${res.statusCode}`))},fail:reject}))}module.exports={request}
