# 外部资源状态

- 甲方代码：GitHub `qingjia2222/MallQuest` main，只有初始化骨架；已并入并补齐最低双端。
- Python：`D:\miniconda\Miniconda3\envs\OpenCV\python.exe`，Python 3.12.13。
- LLM：千问，待用户将 Base URL/Key/Model 写入未跟踪的 `server/.env`；scripted 已作为默认。
- 微信：暂无 AppID/AppSecret；真实路径已实现，当前 mock。
- 商场：`星河里`（暂名）。
- 地图：无真实素材，使用两层 Demo 仿真图。
- TTS：无在线凭证，使用 Windows 本地语音。
- 演示：优先微信开发者工具录屏；真机作为后续网络条件验证。
