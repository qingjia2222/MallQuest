# 老师问答

## 为什么 LLM 不是摆设？
在线模式由千问完成开放式意图理解、上下文承接、槽位提取、Function Calling、真实候选店选择和调整理由生成；后端执行工具、校验店铺 ID、计算走廊路线并持久化。用户可以自然地说“换成晚上”“加上某店”“删掉某店”“把某店放最后”“少走路”或“把最近两套方案对比”，结构化方案会真正同步变化。scripted 是可明确识别的离线兜底，不冒充在线模型。

## LLM 与规则系统如何分工？
LLM 理解、规划、解释和选工具；规则/后端负责身份、数据事实、SQL、确认门和事务写入。为防止“模型文字五站、实际卡片两站”，展示文案只依据后端校验并落库后的 canonical plan 生成，因此文字、卡片、地图和确认事务引用同一组 store_id。

已有方案的追问不是靠为每句话增加 if：LLM 先把自然表达编译为通用 patch operations，包括 add/remove/replace/move/reorder/set_time/update_slots/set_strategy。后端将操作应用到当前 canonical plan，默认保留未被点名的站点和时间，验证真实 store_id、营业状态、活动数量与时间冲突后再重算路线。即使模型偶尔错误返回完整方案，差异校验层也会把它转换为增量操作，避免一句“19:00 再加一顿饭”覆盖原行程或同时安排两家餐厅。

## Function Calling 如何访问私有数据？
工具注册表提供 JSON Schema；调用时后端剥离模型传来的权限字段，再从认证会话注入 mall/user/session，callback 参数化查询 SQLite。

## “一码一所”如何实现？
扫码绑定 session.mall_id，registry 和每条业务 SQL 都以该值过滤；`mall_demo` 与 `mall_alt` 自动测试证明不串数据。

## 为什么显式状态机？
长任务可持久化、刷新可恢复，并能保证 CONFIRM 之前没有事务写入。一次 Prompt 无法可靠表达权限边界和中间状态。

## 写操作为何确认？
预约、券、票会改变数据库。只有明确 decision=confirm 且计划状态为 CONFIRM，后端才进入 EXECUTE。

## RAG 与结构化查询为何分开？
积分规则是文档条款，适合带来源检索；车位、库存、积分余额是实时结构化事实，必须直接查表。

## 地图路线真实吗？
算法和路网是真实 Dijkstra，实现同层/跨层与电梯换乘；底图和坐标是课程 Demo 仿真，不声称真实室内定位。

## 五场景如何复用？
共享一个状态机和执行器；模板仅声明 required slots、store sequence 和 actions，不复制五套流程。

## 在线模型挂掉怎么办？
记录失败类型和 fallback 计数，显式返回 degraded=true，再走 scripted；不吞异常、不伪称在线成功。

## 双端如何共享？
统一 Token/user_id/session_id 和 SQLite。开发 mock 中 Web 与微信映射到同一 `user_demo`。

## 为什么登录入口要拆成三方？
游客、商户、管理者的目标和权限完全不同。启动页先选身份，登录后进入独立工作区且不提供内部角色切换；后端再用登录 channel、`merchant_store_access`、`manager_access` 做第二层校验，避免仅靠隐藏页面形成越权。

## 商户更新如何到游客端？
商户用店铺编码只绑定自己的 store_id，可更新营业、排队、座位和优惠；游客地图热点和公共门店接口读取同一份状态，因此更新后立即可见。本阶段数据是可联调 Mock，接口边界可直接替换为后续数据库。

## 问路动画是真 3D 吗？
当前是可交互 2.5D Demo：SVG 楼层、透视视觉、Dijkstra 路线和 Canvas 红点逐节点播放都是真实实现；没有真实平面图、三维模型和室内定位，所以不声称为生产级 3D。正式前端地图到达后保留同一 route_animation 契约接入。

## 为什么攻略问答不弹地图？
后端先做窄范围目的地意图判断，只对“怎么去/导航到/从…到…”返回导航字段；攻略、优惠、会员等问答走各自工具。自动测试同时验证正触发和不误触发。

## 商场为什么会购买？
同一私域底座连接游客服务、商户自运营和管理驾驶舱：降低问询与导览成本，缩短商户状态同步链路，用优惠、排队与路线提升转化，并把客流与营收形成可视化经营闭环。店铺编码和稳定的数据适配契约让新增商场与商户可复制接入；地图资产由专业前端导览组件承载，后端提供统一店铺绑定与安全路网。

## code2session 做什么？
小程序 `wx.login()` 得到临时 code，后端携 AppID/AppSecret 换取 openid/session_key；session_key 不返回前端、不写日志。

## TTS 如何实现？
课程 Demo 通过 Windows SAPI 生成 WAV，接口返回可播放 URL；若系统语音不可用会明确标记 emergency fallback。

## 安全与压测？
覆盖无 Token、跨用户、跨商场、确认绕过、SQL 注入和 Prompt 注入；scripted 下以 16 并发跑 120 个混合只读请求并如实记录 p95。

## 商业上线还缺什么？
真实商场/票务/停车 API、正式微信主体与 HTTPS、真实地图标定和室内定位、生产密钥管理、审计、限流、高可用数据库与合规评估。
